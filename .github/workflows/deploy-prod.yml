name: Deploy Production

on:
  push:
    branches: ["main"]

jobs:
  prod-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Production deployment workflow to be connected to hosting later."