name: Deploy Staging

on:
  push:
    branches: ["develop"]

jobs:
  staging-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Staging deployment workflow to be connected to hosting later."